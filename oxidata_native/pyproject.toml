[build-system]
requires = ["maturin>=1.5,<2.0"]
build-backend = "maturin"

[project]
name = "oxidata_native"
version = "0.1.0"
requires-python = ">=3.10"

[tool.maturin]
bindings = "pyo3"
module-name = "oxidata_native"
